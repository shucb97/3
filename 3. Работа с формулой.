#include <stdio.h>
#include <math.h>

int main() {
float T,V,a,g;
 printf("Введите T");
scanf("%f", &T);
 printf("Введите V");
scanf("%f", &V);
    g=9.8;
 a =((asin((g*T)/(2*V)))*(180/3.14));
 printf("Угол=%f",a); 

    return 0;
}
